# The requirement list lives in pyproject.toml. Installing the toolbox installs
# everything with it:
#
#     pip install -e .
#
# This file exists only for tools that expect it (the Codespaces devcontainer
# among them) and simply defers to pyproject.toml.
-e .
